package com.mipt.Portal.model.announcement.Classes;


public class AddComment {

  protected void doPost(String comment) {
    try {
      // String author = comment.getParameter("author");
      // String text = comment.getParameter("text");
      // String adId = comment.getParameter("adId");

      // Сохраняем в базу

    } catch (Exception e) {
      e.printStackTrace();
    }
  }
}
